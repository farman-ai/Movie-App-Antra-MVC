trigger:
- main

variables:
  buildConfiguration: 'Release'
  # This matches your Azure Subscription ID
  azureServiceConnection: 'Azure subscription 1(208d95e6-0ef5-4010-a183-3e7c067f8805)'
  # UPDATE THIS to your actual App Service name from the Azure Portal
  appName: 'MovieShop2026-App' 

stages:
- stage: Build
  jobs:
  - job: BuildJob
    pool:
      name: 'Default'
      demands:
      - Agent.Name -equals macs-MacBook-Pro
    
    steps:
    - script: |
        find . -name "*.csproj" -exec dotnet build {} --configuration $(buildConfiguration) \;
      displayName: 'Dotnet Build'

    - script: |
        dotnet publish **/*.csproj --configuration $(buildConfiguration) --output $(Build.ArtifactStagingDirectory)
      displayName: 'Dotnet Publish'

    - task: PublishBuildArtifacts@1
      inputs:
        PathtoPublish: '$(Build.ArtifactStagingDirectory)'
        ArtifactName: 'drop'
        publishLocation: 'Container'

- stage: Deploy
  dependsOn: Build
  condition: succeeded()
  jobs:
  - deployment: DeployJob
    pool:
      vmImage: 'ubuntu-latest'
    environment: 'production'
    strategy:
      runOnce:
        deploy:
          steps:
          - task: AzureWebApp@1
            inputs:
              azureSubscription: '$(azureServiceConnection)'
              appType: 'webApp'
              appName: '$(appName)'
              package: '$(Pipeline.Workspace)/drop/**/*.zip'